import { Typography } from 'antd';

const { Title, Paragraph, Text, Link } = Typography;

export function AdminPage() {
    return(
        <Typography>
            <Title>
                Administração
            </Title>
        </Typography>
    )
}