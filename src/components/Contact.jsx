import { Typography } from 'antd';

const { Title, Paragraph, Text, Link } = Typography;

export function Contact() {
    return(
        <Typography>
            <Title>
                Contatos
            </Title>
        </Typography>
    )
}